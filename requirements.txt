# =============================================================================
# requirements.txt — Capstone3 SER Project
#
# INSTALLATION ORDER MATTERS — follow the steps below exactly.
#
# Step 1: Install PyTorch with CUDA 12.1 (GPU support for RTX 3050)
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Step 2: Install NumPy FIRST, before librosa (Numba version constraint)
#   pip install "numpy<2.4"
#
# Step 3: Install everything else
#   pip install -r requirements.txt
#
# Step 4: Verify GPU works
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#
# If you get a Numba/NumPy error when importing librosa:
#   pip install "numpy<2.4"   (downgrade if needed)
# =============================================================================

# ---- Core ML & Deep Learning ----
# NOTE: torch, torchvision, torchaudio must be installed via the CUDA URL above.
# Do NOT install them from this file or you will get the CPU-only version.
# torch>=2.0.0          # installed via CUDA URL — do not re-install here
# torchvision>=0.15.0   # installed via CUDA URL — do not re-install here
# torchaudio>=2.0.0     # installed via CUDA URL — do not re-install here

scikit-learn>=1.3.0

# ---- Audio Processing ----
# numpy MUST be <2.4 for librosa's numba dependency
numpy<2.4
librosa>=0.10.0
soundfile>=0.12.1       # backend for librosa audio I/O on Windows

# ---- Data Handling ----
pandas>=2.0.0

# ---- Visualization ----
matplotlib>=3.7.0
seaborn>=0.12.0

# ---- Utilities ----
tqdm>=4.65.0            # progress bars (optional but useful during extraction)

# ---- Deployment (install when you reach the Gradio app step) ----
gradio>=4.0.0
